# Copy this file to .env.local and fill in your actual values

# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# Database URL for Drizzle (for migrations and server-side operations)
DATABASE_URL=postgresql://postgres:[password]@db.[project-ref].supabase.co:5432/postgres

# To get these values:
# 1. Go to your Supabase project dashboard
# 2. Go to Settings > API
# 3. Copy the Project URL and anon/public key
# 4. For the service role key, copy the service_role key (keep this secret!)
# 5. For DATABASE_URL, go to Settings > Database and copy the connection string
